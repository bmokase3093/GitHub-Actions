name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Printing greeting
        run: echo "Hello world!"
      - name: Print goodbye
        run: echo "Done - bye!"
      - name: Run multiple shell comands
        run: |
            echo "First"
            echo "Second"
            echo "Third"
